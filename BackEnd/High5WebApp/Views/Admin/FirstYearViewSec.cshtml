@model IEnumerable<BackEnd.Services.Models.Entities.Section>

@{
    ViewBag.Title = "FirstYearViewSec";
}

<h2>FirstYearViewSec</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearId)
            @Html.DisplayFor(modelItem => item.SecTeachersId)


        </td>
    </tr>
}

</table>
